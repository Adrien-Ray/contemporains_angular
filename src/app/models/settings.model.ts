export class Settings {
    constructor(
        public id: number,
        public attributes: {
            content_landing: string,
            createdAt: string,
            updatedAt: string,
        },
    ) { }
}
